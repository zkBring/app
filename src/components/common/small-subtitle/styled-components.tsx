import styled from "styled-components"

export const SubtitleComponent = styled.h4`
  color: ${props => props.theme.primaryTextColor};
  margin: 0;
  font-size: 15px;
  line-height: 26px;
  text-align: center;
  font-weight: 400;
`
